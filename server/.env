# This is the main .env file for the project.
# It can contain environment variables used by both frontend and backend,
# or general project-wide settings.

# Example:
# DATABASE_URL="postgresql://user:password@host:port/database"
# API_KEY="your_api_key_here"

# For the backend, specific environment variables are often placed in server/.env
# For the frontend, variables prefixed with VITE_ are exposed to the browser.

# Frontend environment variables (Vite)
VITE_API_BASE_URL=/api

# Backend environment variables
SQLALCHEMY_DATABASE_URL=mysql+mysqlconnector://user:password@db:3306/gilab